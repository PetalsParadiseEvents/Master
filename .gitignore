# Node / build artifacts
node_modules/

# OS artifacts
.DS_Store

# API secrets — these files contain the actual API key via SetEnv
# They must be uploaded to the server manually, NEVER pushed to GitHub
api/.htaccess
