# Node / build artifacts
node_modules/

# OS artifacts
.DS_Store

# API secrets — these files contain the actual API key
# They must be created manually on the server, NEVER pushed to GitHub
api/.htaccess
api/secrets.php
